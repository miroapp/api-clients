/**
 * Platform Tags
 * SCIM V1 API implemented by Miro
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import fetch, {Response} from 'node-fetch';
import * as http from 'http';

/* tslint:disable:no-unused-locals */
import { ErrorResponse } from '../model/errorResponse';
import { UpdateUserRequest } from '../model/updateUserRequest';
import { UserListResponse } from '../model/userListResponse';
import { UserSchema } from '../model/userSchema';

import { ObjectSerializer } from '../model/models';

import { HttpError } from './apis';

let defaultBasePath = 'https://api.miro.com';

// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================


export interface UserApiMethods {
    scimCreateUser(xClientType?: string, userSchema?: UserSchema, ): Promise<{ response: Response; body: UserSchema;  }>
    scimDeleteUser(userId: number, ): Promise<{ response: Response; body?: any;  }>
    scimGetUser(userId: number, ): Promise<{ response: Response; body: UserSchema;  }>
    scimGetUsers(attributes?: string, filter?: string, startIndex?: number, count?: number, sortBy?: string, sortOrder?: 'ascending' | 'descending', ): Promise<{ response: Response; body: UserListResponse;  }>
    scimReplaceUser(userId: number, xClientType?: string, userSchema?: UserSchema, ): Promise<{ response: Response; body: UserSchema;  }>
    scimUpdateUser(userId: number, xClientType?: string, updateUserRequest?: UpdateUserRequest, ): Promise<{ response: Response; body: UserSchema;  }>
}

export function UserApi (accessToken: string|(() => Promise<string>), basePath: string = defaultBasePath, logger?: (...thing: any) => void): UserApiMethods {
    return {
        /**
         * Creates a new user.
         * @param xClientType 
         * @param userSchema Payload must include &lt;b&gt;userName&lt;/b&gt; attribute populated with an email address, &lt;b&gt;familyName&lt;/b&gt;, and &lt;b&gt;givenName&lt;/b&gt; or a &lt;b&gt;displayName&lt;/b&gt; attribute. If displayName is not populated, a combination of givenName and familyName is used.
         */
        scimCreateUser: async function (xClientType?: string, userSchema?: UserSchema, ) : Promise<{ response: Response; body: UserSchema;  }> {
            const localVarPath = '/scim/Users';
            let localVarQueryParameters = new URLSearchParams();
            let localVarHeaderParams: Record<string, string> = {}
            const produces = ['application/json', 'application/scim+json'];
            // give precedence to 'application/json'
            if (produces.indexOf('application/json') >= 0) {
                localVarHeaderParams.Accept = 'application/json';
            } else {
                localVarHeaderParams.Accept = produces.join(',');
            }


            const resource = new URL(localVarPath, basePath)
            resource.search = localVarQueryParameters.toString()


const options = {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${typeof accessToken === 'function' ? await accessToken() : accessToken}`
                },
                body: JSON.stringify(ObjectSerializer.serialize(userSchema, "UserSchema"))
            }

            if (typeof logger === 'function') logger('FETCH', resource.toString(), options)

            const response = await fetch(resource, options)

            if (typeof logger === 'function') logger('RESPONSE', response)

            let bodyAsJson: unknown
            try {
                bodyAsJson = await response.json()
            } catch (err) {
                // Body doesn't have valid json
            }

            if (!response.ok) {
                throw new HttpError(response, bodyAsJson, response.status)
            }

            const body = ObjectSerializer.deserialize(bodyAsJson, "UserSchema");

            return({response, body})
        },
        /**
         * Deletes a single user from the organization. A user who is the last admin in the team or the last admin in the organization cannot be deleted.
         * @param userId 
         */
        scimDeleteUser: async function (userId: number, ) : Promise<{ response: Response; body?: any;  }> {
            const localVarPath = '/scim/Users/{userId}'
                .replace('{' + 'userId' + '}', encodeURIComponent(String(userId)));
            let localVarQueryParameters = new URLSearchParams();
            let localVarHeaderParams: Record<string, string> = {}
            const produces = ['application/json', 'application/scim+json'];
            // give precedence to 'application/json'
            if (produces.indexOf('application/json') >= 0) {
                localVarHeaderParams.Accept = 'application/json';
            } else {
                localVarHeaderParams.Accept = produces.join(',');
            }

            // verify required parameter 'userId' is not null or undefined
            if (userId === null || userId === undefined) {
                throw new Error('Required parameter userId was null or undefined when calling scimDeleteUser.');
            }


            const resource = new URL(localVarPath, basePath)
            resource.search = localVarQueryParameters.toString()


const options = {
                method: 'DELETE',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${typeof accessToken === 'function' ? await accessToken() : accessToken}`
                },
            }

            if (typeof logger === 'function') logger('FETCH', resource.toString(), options)

            const response = await fetch(resource, options)

            if (typeof logger === 'function') logger('RESPONSE', response)

            let bodyAsJson: unknown
            try {
                bodyAsJson = await response.json()
            } catch (err) {
                // Body doesn't have valid json
            }

            if (!response.ok) {
                throw new HttpError(response, bodyAsJson, response.status)
            }

            const body = bodyAsJson

            return({response, body})
        },
        /**
         * Retrieves a single user resource
         * @param userId 
         */
        scimGetUser: async function (userId: number, ) : Promise<{ response: Response; body: UserSchema;  }> {
            const localVarPath = '/scim/Users/{userId}'
                .replace('{' + 'userId' + '}', encodeURIComponent(String(userId)));
            let localVarQueryParameters = new URLSearchParams();
            let localVarHeaderParams: Record<string, string> = {}
            const produces = ['application/json', 'application/scim+json'];
            // give precedence to 'application/json'
            if (produces.indexOf('application/json') >= 0) {
                localVarHeaderParams.Accept = 'application/json';
            } else {
                localVarHeaderParams.Accept = produces.join(',');
            }

            // verify required parameter 'userId' is not null or undefined
            if (userId === null || userId === undefined) {
                throw new Error('Required parameter userId was null or undefined when calling scimGetUser.');
            }


            const resource = new URL(localVarPath, basePath)
            resource.search = localVarQueryParameters.toString()


const options = {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${typeof accessToken === 'function' ? await accessToken() : accessToken}`
                },
            }

            if (typeof logger === 'function') logger('FETCH', resource.toString(), options)

            const response = await fetch(resource, options)

            if (typeof logger === 'function') logger('RESPONSE', response)

            let bodyAsJson: unknown
            try {
                bodyAsJson = await response.json()
            } catch (err) {
                // Body doesn't have valid json
            }

            if (!response.ok) {
                throw new HttpError(response, bodyAsJson, response.status)
            }

            const body = ObjectSerializer.deserialize(bodyAsJson, "UserSchema");

            return({response, body})
        },
        /**
         * Retrieves the list of users in your organization. <br><b>Note</b>: Users with role \"ORGANIZATION_EXTERNAL_USER\" or \"ORGANIZATION_TEAM_GUEST_USER\" are not retrieved in this request
         * @param attributes A comma-separated list of attribute names to return in the response. Example attributes&#x3D;id,displayName
         * @param filter You can request a subset of resources by specifying the filter query parameter containing a filter expression. Attribute names and attribute operators used in filters are case insensitive. The filter parameter must contain at least one valid expression. Each expression must contain an attribute name followed by an attribute operator and an optional value. &lt;br&gt;eq &#x3D; equal&lt;br&gt; ne &#x3D; not equal&lt;br&gt; co &#x3D; contains&lt;br&gt; sw &#x3D; starts with&lt;br&gt; ew &#x3D; ends with&lt;br&gt; pr &#x3D; preset (has value)&lt;br&gt; gt &#x3D; greater than&lt;br&gt; ge &#x3D; greater than or equal to&lt;br&gt; lt &#x3D; less than&lt;br&gt; le &#x3D; less than or equal to&lt;br&gt; and &#x3D; Logical \&quot;and\&quot;&lt;br&gt; or &#x3D; Logical \&quot;or\&quot;&lt;br&gt; not &#x3D; \&quot;Not\&quot; function&lt;br&gt; () &#x3D; Precedence grouping &lt;br&gt;The value must be passed within parenthesis. &lt;br&gt;Example: filter&#x3D;(userName eq \&quot;user@miro.com\&quot;)
         * @param startIndex Start index (1-based). Example startIndex&#x3D;1
         * @param count Specifies the desired maximum number of query results per page. Example count&#x3D;12
         * @param sortBy Specifies the attribute whose value will be used to order the response. Example sortBy&#x3D;userName
         * @param sortOrder Defines the order in which the \&#39;sortBy\&#39; parameter is applied. Allowed values are \&#39;ascending\&#39; and \&#39;descending\&#39;. Example sortOrder&#x3D;ascending
         */
        scimGetUsers: async function (attributes?: string, filter?: string, startIndex?: number, count?: number, sortBy?: string, sortOrder?: 'ascending' | 'descending', ) : Promise<{ response: Response; body: UserListResponse;  }> {
            const localVarPath = '/scim/Users';
            let localVarQueryParameters = new URLSearchParams();
            let localVarHeaderParams: Record<string, string> = {}
            const produces = ['application/json', 'application/scim+json'];
            // give precedence to 'application/json'
            if (produces.indexOf('application/json') >= 0) {
                localVarHeaderParams.Accept = 'application/json';
            } else {
                localVarHeaderParams.Accept = produces.join(',');
            }

            if (attributes !== undefined) {
                localVarQueryParameters.append('attributes', ObjectSerializer.serialize(attributes, "string"));
            }

            if (filter !== undefined) {
                localVarQueryParameters.append('filter', ObjectSerializer.serialize(filter, "string"));
            }

            if (startIndex !== undefined) {
                localVarQueryParameters.append('startIndex', ObjectSerializer.serialize(startIndex, "number"));
            }

            if (count !== undefined) {
                localVarQueryParameters.append('count', ObjectSerializer.serialize(count, "number"));
            }

            if (sortBy !== undefined) {
                localVarQueryParameters.append('sortBy', ObjectSerializer.serialize(sortBy, "string"));
            }

            if (sortOrder !== undefined) {
                localVarQueryParameters.append('sortOrder', ObjectSerializer.serialize(sortOrder, "'ascending' | 'descending'"));
            }


            const resource = new URL(localVarPath, basePath)
            resource.search = localVarQueryParameters.toString()


const options = {
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${typeof accessToken === 'function' ? await accessToken() : accessToken}`
                },
            }

            if (typeof logger === 'function') logger('FETCH', resource.toString(), options)

            const response = await fetch(resource, options)

            if (typeof logger === 'function') logger('RESPONSE', response)

            let bodyAsJson: unknown
            try {
                bodyAsJson = await response.json()
            } catch (err) {
                // Body doesn't have valid json
            }

            if (!response.ok) {
                throw new HttpError(response, bodyAsJson, response.status)
            }

            const body = ObjectSerializer.deserialize(bodyAsJson, "UserListResponse");

            return({response, body})
        },
        /**
         * Updates an existing user resource. It is the easiest way to replace the user information.
         * @param userId 
         * @param xClientType 
         * @param userSchema 
         */
        scimReplaceUser: async function (userId: number, xClientType?: string, userSchema?: UserSchema, ) : Promise<{ response: Response; body: UserSchema;  }> {
            const localVarPath = '/scim/Users/{userId}'
                .replace('{' + 'userId' + '}', encodeURIComponent(String(userId)));
            let localVarQueryParameters = new URLSearchParams();
            let localVarHeaderParams: Record<string, string> = {}
            const produces = ['application/json', 'application/scim+json'];
            // give precedence to 'application/json'
            if (produces.indexOf('application/json') >= 0) {
                localVarHeaderParams.Accept = 'application/json';
            } else {
                localVarHeaderParams.Accept = produces.join(',');
            }

            // verify required parameter 'userId' is not null or undefined
            if (userId === null || userId === undefined) {
                throw new Error('Required parameter userId was null or undefined when calling scimReplaceUser.');
            }


            const resource = new URL(localVarPath, basePath)
            resource.search = localVarQueryParameters.toString()


const options = {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${typeof accessToken === 'function' ? await accessToken() : accessToken}`
                },
                body: JSON.stringify(ObjectSerializer.serialize(userSchema, "UserSchema"))
            }

            if (typeof logger === 'function') logger('FETCH', resource.toString(), options)

            const response = await fetch(resource, options)

            if (typeof logger === 'function') logger('RESPONSE', response)

            let bodyAsJson: unknown
            try {
                bodyAsJson = await response.json()
            } catch (err) {
                // Body doesn't have valid json
            }

            if (!response.ok) {
                throw new HttpError(response, bodyAsJson, response.status)
            }

            const body = ObjectSerializer.deserialize(bodyAsJson, "UserSchema");

            return({response, body})
        },
        /**
         * Updates an existing user resource, overwriting values for specified attributes. Attributes that are not provided will remain unchanged. PATCH only updates the fields provided.
         * @param userId 
         * @param xClientType 
         * @param updateUserRequest The body of a PATCH request must contain the attribute Operations, whose value is an array of one or more PATCH operations. Each PATCH operation object must have exactly one op member.
         */
        scimUpdateUser: async function (userId: number, xClientType?: string, updateUserRequest?: UpdateUserRequest, ) : Promise<{ response: Response; body: UserSchema;  }> {
            const localVarPath = '/scim/Users/{userId}'
                .replace('{' + 'userId' + '}', encodeURIComponent(String(userId)));
            let localVarQueryParameters = new URLSearchParams();
            let localVarHeaderParams: Record<string, string> = {}
            const produces = ['application/json', 'application/scim+json'];
            // give precedence to 'application/json'
            if (produces.indexOf('application/json') >= 0) {
                localVarHeaderParams.Accept = 'application/json';
            } else {
                localVarHeaderParams.Accept = produces.join(',');
            }

            // verify required parameter 'userId' is not null or undefined
            if (userId === null || userId === undefined) {
                throw new Error('Required parameter userId was null or undefined when calling scimUpdateUser.');
            }


            const resource = new URL(localVarPath, basePath)
            resource.search = localVarQueryParameters.toString()


const options = {
                method: 'PATCH',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${typeof accessToken === 'function' ? await accessToken() : accessToken}`
                },
                body: JSON.stringify(ObjectSerializer.serialize(updateUserRequest, "UpdateUserRequest"))
            }

            if (typeof logger === 'function') logger('FETCH', resource.toString(), options)

            const response = await fetch(resource, options)

            if (typeof logger === 'function') logger('RESPONSE', response)

            let bodyAsJson: unknown
            try {
                bodyAsJson = await response.json()
            } catch (err) {
                // Body doesn't have valid json
            }

            if (!response.ok) {
                throw new HttpError(response, bodyAsJson, response.status)
            }

            const body = ObjectSerializer.deserialize(bodyAsJson, "UserSchema");

            return({response, body})
        },
    }
}
