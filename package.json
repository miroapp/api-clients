{
  "name": "rest-api-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "openapi-generator-cli generate -i '../miro-openapi-files/openapi/merged.json' -o typescript-node -g typescript-node -t template -p npmName=miro-api && cd typescript-node && npm i && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1"
  }
}
