{
  "name": "rest-api-client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pregenerate": "rm -rf tmp",
    "generate": "openapi-generator-cli generate -i '../miro-openapi-files/openapi/merged.json' -o tmp -g typescript-node -t template && tsc tmp/api.ts --outDir ./node/ --lib ES2021 --declaration",
    "postgenerate": "rm -rf tmp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1",
    "esbuild": "^0.14.50",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "@types/node": "^18.7.1"
  }
}
